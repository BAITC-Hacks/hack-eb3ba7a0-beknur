{
  "name": "akim-five-hours",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
